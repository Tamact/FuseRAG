from api import app


def main() -> None:
    """
    Entry point kept intentionally minimal.
    Run the API with:
      uv run uvicorn main:app --reload
    """
    pass


if __name__ == "__main__":
    main()
