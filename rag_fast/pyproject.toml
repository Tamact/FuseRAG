[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "rag-fast"
version = "0.1.0"
description = "Rust accelerators for RAG (PyO3)"
requires-python = ">=3.10"

[tool.maturin]
module-name = "rag_fast"
bindings = "pyo3"
